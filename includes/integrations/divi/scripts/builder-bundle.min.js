class ConvertKitForm extends Component {

  static slug = 'convertkit_form';

  render() {
    return React.createElement( 'div', null, 'convertkitform' );
  }
  
}

jQuery(window).on('et_builder_api_ready', function ( event, API ) {

	console.log( event );
	console.log( API );

  	// Register custom modules classes @TODO.



  	API.registerModules(modules);
});